#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/* more headers goes there */

/* find the last digit of the given random numbers */
int main(void)
{
    
    int ch;
                                             for(ch = 'a'; ch < 'y';ch++){
                                                putchar(ch);

                                             }
	
	return (0);
}
